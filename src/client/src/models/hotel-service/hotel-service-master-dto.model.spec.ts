import { HotelServiceMasterDTO } from './hotel-service-master-dto.model';

describe('HotelServiceMasterDtoModel', () => {
  it('should create an instance', () => {
    expect(new HotelServiceMasterDTO()).toBeTruthy();
  });
});
